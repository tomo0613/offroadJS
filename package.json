{
  "name": "vehicle-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server/httpServer.js",
    "client": "browser-sync start --server --startPath './client/' --watch",
    "controller": "browser-sync start -s -w --startPath './controller/'",
    "server": "nodemon --ext js --exec 'node server/httpServer.js' ",
    "nodemon": "nodemon --ext js --exec \"node\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "browser-sync": "^2.26.3",
    "nodemon": "^1.18.4"
  },
  "dependencies": {
    "ws": "^6.1.0"
  }
}
